[project]
name = "personalized-chess-ai"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "python-chess>=1.999",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
